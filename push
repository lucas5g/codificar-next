git add .
git commit -m "$1"
git push
ssh -t versionaplicativoderestaurante "\
    cd codificar/codificar-next/ \
    && git pull \
    && source ~/.nvm/nvm.sh \
    && nvm use 16 \
    && npm run build  \
    && pm2 restart codificar --update-env  \
"
echo ""
echo "http://version.aplicativoderestaurante.com.br:3000"
    # && git pull \
# ssh -t versionaplicativoderestaurante "cd codificar/codificar-next && npm run build && pm2 restart codificar"
# ssh -t versionaplicativoderestaurante "cd codificar/codificar-next/ && cat .env"

# # echo "test $1"